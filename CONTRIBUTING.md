# Contribuer

Merci de contribuer.

## Installation et usage rapide

```bash
python -m venv .venv
source .venv/bin/activate && pip install -r requirements.txt
pytest
```

Note: Les hooks pre-commit sont configures via `.pre-commit-config.yaml`. Vous pouvez les installer si vous souhaitez les executer automatiquement avant les commits.
